# Common Auth Library
User management, token lifecycle, and persistence are handled centrally through this common auth library, allowing consistent authentication behavior across applications.

## Features
- ASP.NET Core Identity
- Entity Framework
- JWT bearer authentication
- Custom email and password sign in
- Google OAuth sign-in
